on:
  push:
    branches:
      - main
      - master

name: Deploy to shinyapps.io

jobs:
  Deploy-shinyapps:
    runs-on: Ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
        with:
          driver: docker

      - name: Cache Docker layers
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      - name: DockerHub Login
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: .
          push: false
          target: shinyapps
          tags: shinyapps:latest

      - name: Deploy
        env:
          SHINYAPPS_APPNAME: "wheretowork"
        run: |
          docker run \
          -e SHINYAPPS_USER=${{ secrets.SHINYAPPS_USER }} \
          -e SHINYAPPS_TOKEN=${{ secrets.SHINYAPPS_TOKEN }} \
          -e SHINYAPPS_SECRET=${{ secrets.SHINYAPPS_SECRET }} \
          -e SHINYAPPS_APPNAME=$SHINYAPPS_APPNAME \
          shinyapps:latest

      - name: Move cache
        run: |
          rm -rf /tmp/.buildx-cache
          mv /tmp/.buildx-cache-new /tmp/.buildx-cache
